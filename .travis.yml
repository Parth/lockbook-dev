install: true

os: linux
dist: trusty
jdk: oraclejdk11

script:
  - ./gradlew runtime

deploy:
    before_deploy: "./gradlew runtimeZip"
    provider: releases
    api_key: $RELEASE_TOKEN
    file: build/image.zip
    on:
        tags: false
